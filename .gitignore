# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli
expo-env.d.ts
# @end expo-cli

# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
android/app/build/
android/build/
android/.gradle/
android/app/.gradle/
android/.kotlin/
android/cxx/

# Local Expo config
.expo/
.expo-shared/
expo-env.d.ts

# Web bundle embedded in APK — regenerable, run `npx expo export` to refresh
android/app/src/main/assets/web/
android/app/build/generated/assets/web/
android/app/build/intermediates/merged_assets/

# iOS (not used on this repo right now, but ignore in case)
ios/Pods/
ios/build/
ios/Podfile.lock

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.local
.env.*.local

# Misc
*.tsbuildinfo
.eslintcache
.cache/

# Compiled-output leftovers inside TS folders (tsc -p, etc.)
services/**/*.js
!services/**/*.config.js

# SQLite user databases — never commit personal data
*.db
*.db-journal
*.sqlite
*.sqlite-journal

# The `build/generated/assets/web/` is the same as `dist/` content that
# gets re-generated by gradle's `copyWebBundle` task. Same for the
# intermediate merged_assets folders. They are derivable, never edit by hand.
